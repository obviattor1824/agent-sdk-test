# setting_sources=[] requires SDK > 0.1.59
claude-agent-sdk>0.1.59
fastapi
uvicorn
# app.py imports httpx directly. It also arrives via claude-agent-sdk -> mcp,
# but the UI is meant to be installable without the SDK.
httpx
# st.image(width="stretch") requires 1.49
streamlit>=1.49
